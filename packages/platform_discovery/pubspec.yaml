name: streamsize_platform_discovery
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.3.0

dependencies:
  flutter:
    sdk: flutter
  streamsize_core:
    path: ../core

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^5.0.0
